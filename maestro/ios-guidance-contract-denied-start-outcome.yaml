appId: host.exp.Exponent
---
- extendedWaitUntil:
    visible: "This route closed because its workspace is no longer available."
    timeout: 30000
- assertVisible:
    id: "guest-map-primary-action"
- extendedWaitUntil:
    visible:
      id: "workspace-access-refresh"
    timeout: 30000
- extendedWaitUntil:
    visible: "Refresh workspace access"
    timeout: 30000
- assertNotVisible: "Refreshing workspace access"
- assertNotVisible:
    id: "safe-route-live-map"
- assertNotVisible:
    id: "safe-route-stop-action"
- assertNotVisible:
    id: "safe-route-resume-action"
- assertNotVisible:
    id: "safe-route-remaining-metrics"
