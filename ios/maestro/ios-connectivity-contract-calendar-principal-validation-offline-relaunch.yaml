appId: host.exp.Exponent
---
- runFlow: subflows/ios-open-expo-project.yaml
- extendedWaitUntil:
    visible:
      id: "safe-route-login-saved-session-retry"
    timeout: 30000
- assertVisible: "SafeRoute could not verify this saved session. Retry or sign in again."
- assertVisible:
    id: "safe-route-login"
- assertVisible:
    id: "safe-route-login-email"
    enabled: true
- assertVisible:
    id: "safe-route-login-saved-session-retry"
    enabled: true
- assertNotVisible:
    id: "guest-map-workspace-selector"
- assertNotVisible: "Workspace, Guidance Operations"
- assertNotVisible: "Workspace, Support Operations"
- assertNotVisible:
    id: "safe-route-card-66b1b2c3d4e5f60718293b40"
- assertNotVisible: "^Cold restart verification v1.*"
- assertNotVisible:
    id: "safe-route-picker"
- assertNotVisible:
    id: "safe-route-live-map"
- assertNotVisible:
    id: "safe-route-suspended-navigation"
- assertNotVisible:
    id: "safe-route-resume-action"
- takeScreenshot: "connectivity-contract-calendar-principal-validation-offline-relaunch"
