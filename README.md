# SafeRoute Mobile

Platform applications are organized independently:

- [`ios/`](ios/) — the current SafeRoute iOS application, configuration, tests, and runtime tooling.
- [`android/`](android/) — reserved for the future Android application. It intentionally contains only a version-control placeholder today.

Run current development and test commands from `ios/`.
